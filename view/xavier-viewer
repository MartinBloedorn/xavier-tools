#!/usr/bin/env python3
"""Entry point for the xavier EEG viewer.

Kept as a thin shim so the package can also be run as
``python -m viewer`` from this directory.
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from viewer.app import main  # noqa: E402  (after the path fix, deliberately)

if __name__ == "__main__":
    sys.exit(main())
